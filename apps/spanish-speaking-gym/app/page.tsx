export default function HomePage() {
  return (
    <section className="card">
      <h2>Welcome</h2>
      <p>
        MVP mode keeps everything free-first: no required paid APIs, no required authentication,
        and no required cloud database.
      </p>
    </section>
  );
}
