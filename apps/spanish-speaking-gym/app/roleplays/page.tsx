export default function RoleplaysPage() {
  return (
    <section className="card">
      <h2>Roleplays</h2>
      <p>
        This section works with local defaults. Any voice, sync, or external integrations remain
        optional and can be enabled later in settings.
      </p>
    </section>
  );
}
